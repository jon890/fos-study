# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

개인 기술 블로그 겸 학습 기록 저장소. 마크다운 파일로 구성되며 GitHub 자동 sync를 통해 블로그에 게시된다. 빌드/테스트 명령어 없음 — 파일 작성과 구조 관리가 주 작업이다.

학습 문서는 마크다운으로만 관리한다.


HTML·PDF 등 검토용 미리보기 산출물은 저장소 밖의 `/private/tmp/` 아래 작업별 임시 디렉터리에 생성하고 Git에 포함하지 않는다.


검토가 끝난 임시 산출물은 필요에 따라 제거한다.

## 폴더 구조와 배치 기준

```
fos-study/
├── task/               # 회사 업무 기록 (팀별 하위 디렉터리)
├── resume/             # 개인 이력서·경력기술서·포트폴리오 보관과 갱신
├── architecture/       # 설계 패턴, 원칙 등 언어/기술 독립적 개념
├── database/           # 데이터 스토어 전반
│   ├── mysql/
│   ├── opensearch/
│   └── redis/
├── java/               # Java 언어 및 Spring 생태계
│   ├── spring/
│   ├── spring-batch/
│   └── ...
├── devops/             # 인프라, 배포, 모니터링
│   ├── docker/
│   ├── k8s/
│   └── monitoring/
├── kafka/              # 메시지 브로커 (database가 아닌 최상위 유지)
├── network/            # HTTP, 인터넷 프로토콜 등
├── algorithm/
├── AI/
└── ...
```

### 폴더 배치 결정 기준

- **database/** — 데이터를 저장·검색하는 스토어. MySQL, OpenSearch, Redis 포함. Kafka는 메시지 브로커라 최상위 유지
- **task/** — 실제 업무 구현 경험. 개념 설명 아닌 "내가 이걸 왜, 어떻게 만들었나" 기록
- **resume/** — 개인 이력을 보관하고 계속 갱신하는 이력서·경력기술서·포트폴리오
- **architecture/** — 특정 기술에 종속되지 않는 설계 개념 (전략 패턴 등). 실제 사례 링크는 포함하되 사례 본문은 `task/`에 작성
- 신규 기술 문서는 기존 최상위 폴더에 맞는 게 없을 때만 새 폴더 생성

### resume/ 보존 예외

`resume/`는 공개 학습 문서와 목적이 다르더라도 개인 이력을 보관하고 갱신하기 위해 의도적으로 유지한다.
학습 저장소의 목적에 맞지 않거나 면접 준비 흔적이 있다는 이유만으로 `resume/` 문서를 삭제하거나 저장소 밖으로 이동하지 않는다.
`resume/` 문서의 삭제·외부 이동·구버전 정리는 사용자가 명시적으로 요청한 범위에서만 수행한다.

## 카테고리와 Frontmatter

글의 카테고리는 기본적으로 **최상위 폴더**로 정해진다 (예: `AI/RAG/intro.md` → AI 카테고리).
한 글을 다른 카테고리에도 노출하려면 글 최상단에 YAML frontmatter 로 `categories` 를 추가한다.
frontmatter 는 선택이며, 없으면 폴더 카테고리 하나로 동작한다.

```yaml
---
categories: [devops, database]
---
```

대표 썸네일이 있으면 마크다운 파일 기준 상대 경로로 `thumbnail`을 추가한다.
썸네일은 글과 같은 디렉터리의 `images/` 아래에 두고 마크다운과 함께 커밋한다.

```yaml
---
thumbnail: ./images/<파일명>-thumbnail.jpg
---
```

- 권장 비율은 16:9이며, 작은 카드에서도 주제가 드러나는 단순한 구도를 사용한다.
- 이미지 안에는 제목, 로고, 워터마크 같은 문자를 넣지 않는다.
- `thumbnail`이 없거나 파일을 읽지 못하면 블로그의 기본 미리보기 이미지를 사용한다.

- 최종 카테고리 = 폴더 카테고리(primary) + `categories` 합집합(중복 제거)
- 위 글이 `AI/...`에 있으면 AI, devops, database 세 카테고리에 모두 노출된다.
- 값은 저장소에 실제로 존재하는 폴더의 상대 경로와 대소문자까지 정확히 일치시킨다.
하위 폴더는 `AI/RAG`처럼 전체 경로를 사용한다.
- 새 최상위 폴더에 글을 하나 이상 추가하면 블로그 카테고리로 자동 등록된다.
별도 코드나 정적 meta 등록은 필요하지 않다.
- 정적 meta가 없는 카테고리는 원래 폴더명, 기본 폴더 아이콘, 이름 기반의 안정적인 색상으로 표시된다.

## 문서 작성 규칙

### 개념 문서 vs 업무 기록 분리

- `architecture/`, `database/`, `java/` 등: 기술 개념 설명. 언제 읽어도 유효한 내용
- `task/<팀명>/`: 특정 시점의 구현 경험. 진행 기간 헤더 필수, 민감 정보 제거

### 민감 정보 제거 기준

업무 기록(`task/`) 작성 시:

- 회사명, 팀명, 내부 시스템 이름 → 일반 표현으로 대체 
- 내부 URL, IP → 제거 또는 `<내부-엔드포인트>` 로 대체

### 코드 예시 검증

문서에 코드 블록을 포함할 때는 반드시 실제 코드에서 클래스명·메서드명·필드명을 확인 후 기재. 

### 학습 문서의 깊이와 시각화

학습 문서는 짧은 요약보다 개념을 다시 공부할 수 있는 충분한 설명을 우선한다.
분량을 줄이기 위한 삭제는 하지 않고, 같은 결론을 반복하는 문장과 역할이 겹치는 예제만 정리한다.

- 기본 개념, 작동 원리, 선택 기준, 트레이드오프, 실패 사례, 검증 방법을 가능한 범위에서 함께 다룬다.
- 흐름, 계층, 상태 변화, 구성요소 관계는 Mermaid·ASCII 도식·표 중 가장 이해하기 쉬운 형태로 시각화한다.
- 단순 장식용 시각화는 만들지 않고, 글만으로 관계를 파악하기 어려운 부분에 사용한다.
- 버전별로 달라지는 동작과 기술 설명은 공식 문서에서 확인하고 적용 버전을 명시한다.
- 잘못된 설명을 발견하면 기존 표현을 보존하지 말고 근거에 맞게 수정한다.

### 마크다운 Bold

`**텍스트(영문)**` 형태는 일부 파서에서 bold가 렌더링되지 않는다.
반드시 `**텍스트**(영문)` 형태로 작성한다.

- 올바른 예: `**구조**(harness)`, `**제어**(Control)`
- 잘못된 예: `**구조(harness)**`, `**제어(Control)**`

메서드명처럼 괄호가 이름의 일부인 경우(`**canRetry()**`)는 코드 인라인으로 표기한다: ``canRetry()``

### 영어 기술 용어 한국어 병기

직관적이지 않은 영어 운영·관측성 용어는 **문서에서 처음 나올 때** `**영어**(한국어)` 형태로 병기한다. 이후로는 영어만 써도 된다.

- 병기 대상 예: `Saturation`(포화도), `Latency`(지연), `Throughput`(처리량), `Backpressure`(배압)
- 예외 (병기 불필요): `RPS`, `p99`, `traceId`, `circuit breaker` 같은 식별자성·관용 용어
- 같은 문서 안에서 1회만 병기. 반복 병기는 장황하다.
- 영어 용어 자체를 한글로 완전히 대체하지 않는다 — 인터뷰·검색·실무에서 쓰는 표준 표기를 유지하기 위함.

### 하위 문서 링크

관련 상세 문서가 저장소 내에 있으면 상대 경로로 링크. 링크 전 파일 존재 여부 확인.

### 가시성 원칙 — 스캔 가능한 형태로 쓴다

문서는 정보 밀도만큼이나 **시각적으로 빠르게 훑을 수 있는가**가 중요하다. 글로 다 풀어 쓰면 정보가 있어도 독자가 못 본다.

- **링크 제목은 짧게, 본문 설명은 다음 줄로 분리** — `[긴 제목 — 부연 설명](path) — 또 긴 줄글` 패턴 금지. 제목은 짧게, 설명은 다음 단락 또는 bullet으로
- **사례 누적이 예상되는 섹션은 H3 또는 표 구조로 시작** — 사례가 1건이어도 미리 `### 사례명` 구조로 잡아두면 누적 시 자연스럽게 확장된다
- **본문과 메타 설명 분리** — "이 글의 X 단락이 정확히 그 환경" 같은 cross-reference 메모는 본문 줄에 묻지 말고 별도 줄로 띄움
- **단락 안에 정보가 4개 이상 묶여 있으면 분해** — 한 paragraph가 정보 항목을 4개 이상 안고 있으면 사람 눈이 흐른다. 헤더·리스트·표 중 하나로 쪼갠다

## task/ 구조

회사 업무 기록을 팀(또는 회사) 단위 디렉터리로 분리한다. 시점·맥락이 묶이는 단위라 폴더 단위로 나누는 것이 검색·민감 정보 관리·기간 표기에 유리하다.

### 팀 디렉터리


| 디렉터리                       | 회사/팀                  | 기간                |
| -------------------------- | --------------------- | ----------------- |
| `task/the-future-company/` | 더퓨쳐컴퍼니                | 2022.02 ~ 2022.11 |
| `task/sb-dev-team/`        | NHN — SB 개발팀 (스포츠 베팅) | 2023.01 ~ 2024.03 |
| `task/nsc-slot/`           | NHN — NSC 슬롯개발팀       | 2024.06 ~ 2025.11 |
| `task/ai-service-team/`    | NHN — AI 서비스 개발팀      | 2025.12 ~         |


### 각 팀 README.md 구성 규칙

- 상단에 **회사·팀·기간·기술 스택** 메타 정보
- **문서 목록 테이블** — 카테고리로 구분: `신규 구현` / `트러블슈팅` / `리팩터링` / `아키텍처 개선`. 카테고리는 강제 아니고 해당 팀에 의미 있는 것만.
- 각 행: `기간 | 업무 한 줄 요약 | 문서 링크`
- 하단에 **기술 키워드** 태그 (선택)

### 문서 작성 규칙

- 진행 기간 헤더 필수 (`**진행 기간**: 2024.06 ~ 2024.12`)
- 회사명·팀명·내부 시스템 이름은 일반 표현으로 대체. 단, README의 메타 정보(회사/팀)는 예외로 표기.
- 내부 운영 중인 상품·서비스명은 공개하지 않고 메커닉/구조 조합으로 일반화 (예: nsc-slot 팀의 슬롯 상품명)
- 새 문서 추가 시 해당 팀 `README.md`의 문서 목록 테이블도 함께 업데이트

### task/ 는 업무 서사, 개념은 기술 폴더에 두고 링크

`task/` 글과 기술 폴더(`devops/`, `network/`, `java/` 등) 글은 **역할을 가른다.** 같은 업무를 양쪽에 쓰면 내용이 갈라지고 어느 쪽을 고쳐야 할지 모르게 된다.


| 위치          | 담는 것                                 |
| ----------- | ------------------------------------ |
| `task/<팀>/` | 왜 이 일을 했나, 어디서 막혔나, **내 기여와 협업**, 회고 |
| 기술 폴더       | 개념·원리·트러블슈팅 상세                       |


task 글에서 개념을 설명하고 싶어지면 **기술 폴더에 쓰고 링크한다.** 개념 문서가 이미 있으면 그걸 링크하고, 없으면 새로 만든 뒤 링크한다. task 글 안에서 개념을 처음부터 풀지 않는다.

권장 구성이다. 항목은 업무에 따라 빼도 된다.

1. **왜 했나** — 배경과 문제
2. **막혔던 지점** — 예상 못 한 것들. 이 글의 핵심
3. **검증 방법** — 어떻게 확인했나
4. **내 기여와 협업** — 내가 한 것, 넘긴 것, 다른 팀과 맞춘 것
5. **회고** — 다음에 다르게 할 것
6. **배운 개념 링크** — 기술 폴더 문서 인덱스

한 업무가 기술 폴더에서 여러 편의 연재로 이미 정리돼 있다면, task 글은 **총괄과 인덱스 역할만** 한다. 상세를 다시 옮겨 적지 않는다.

## Git 커밋 규칙

커밋은 항상 **관심사 단위로 나눠서 원자적으로** 만든다. 한 커밋은 하나의 논리적 변경만 담는다.
